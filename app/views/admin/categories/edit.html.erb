<% provide(:button_text, t("admin.categories.edit.submit")) %>
<div class="col-md-12">
  <%= render  "shared/error_messages" %>
  <div class="card">
    <div class="card-header" data-background-color="purple">
       <h4 class="title"><%= t "admin.categories.edit.title" %></h4>
    </div>
    <div class="card-content">
        <%= render "form" %>
    </div>
  </div>
</div>
