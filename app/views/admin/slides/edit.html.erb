<% provide(:brown_image, t("admin.slide.edit.brown_image")) %>
<% provide(:submit, t("admin.slide.edit.submit")) %>
<div class="col-md-5">
  <%= render "shared/flash_messages"%>
  <%= render  "shared/error_messages" %>
  <div class="card">
    <div class="card-header" data-background-color="purple">
       <h4 class="title"><%= t "admin.slide.edit.title" %></h4>
    </div>
    <div class="card-content">
        <%= render "form" %>
    </div>
  </div>
</div>
<%= render "table" %>
